name: vzon-api
root: ~/Projects/v2v/vzon-api

windows:
  - editor:
      layout: main-vertical
      panes:
        - nvim
  - terminal:
  - postgres:
    - pkill postgres
    - ~/.asdf/shims/postgres
  - redis:
    - pkill redis
    - redis-server
  - elasticsearch:
      - cd vendor/elasticsearch
      - ./bin/elasticsearch
  - kibana:
      - cd vendor/kibana
      - ./bin/kibana
